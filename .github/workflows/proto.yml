name: proto is updated

on:
  push:
    paths:
      - proto/**
  pull_request:
    paths:
      - proto/**

jobs:
  slack-notification:
    runs-on: ubuntu-latest
    steps:
      - name: Slack Notification
        run: |
          curl -X POST \
            --data-urlencode "payload={\"text\": \"integration.proto has been updated\"}" \
            ${{ secrets.VBK_ALT_PROTO_WEBHOOK }}
